const setup = () => {
    let geschiedenis = [];
    let invoer = document.getElementsById("zoekInput").value();

    switch (invoer) {
        case 'g':
            url =
                break;
        case 'y':
            url =
                break;
        case 't':
            url =
                break;
        case 'i':
            ulr =
                break;
        default:
            alert('De prefix is niet gekend.');
            return;
    }

}
window.addEventListener("load", setup);